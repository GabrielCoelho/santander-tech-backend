const number1 = 100;
const number2 = 20;

// `==` doesn't compare types.
if (number2 === number1) {
  console.log(`${number2} is equal to ${number1}`);
} else {
  console.log(`${number2} is not equal to ${number1}`);
}
